# lore_rendering
It renders lore.

Use `scale` to enlarge the rendered image linearly, for example `sync_render(text, scale=2)`.

Use `generate_gif=False` to force a single image output even when the text contains random formatting.

